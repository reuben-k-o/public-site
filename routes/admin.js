const express = require("express");
const router = express.Router();

const adminController = require("../controllers/progress");

// router.get("/admin", adminController.getAdmin);

module.exports = router;
